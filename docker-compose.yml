version: '3.8'

services:
  backend:
    build: ./back
    ports:
      - "5002:3000"
    environment:
      - NODE_ENV=production
    restart: always

  frontend:
    build: ./front
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    restart: always

  admin:
    build: ./brzk_admin
    ports:
      - "5173:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    restart: always 